// No-op stub for standalone export (was Lovable-specific in the original project).
export function reportLovableError(_error: unknown, _context?: Record<string, unknown>) {
  // intentionally empty
}
